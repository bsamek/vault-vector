# Agent notes

## docs/superpowers/

`docs/superpowers/` is listed in `.gitignore` and is not committed to the repository. It contains local-only planning artifacts (design specs and implementation plans generated during development). The files exist on disk but should never be staged or committed.
